name: seed

on:
  push:
    branches:
      - master
    paths-ignore:
      - 'README.md'

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: checkout source code
        uses: actions/checkout@master
      - name: Build CI_LAST_RUN.txt
        run: echo "$(date -u +'%Y-%m-%dT%H:%M:%SZ') ${GITHUB_SHA}" > CI_LAST_RUN.txt
        if: always()
      - name: Trigger job 01
        uses: stefanzweifel/git-auto-commit-action@v2.5.0
        with:
          commit_message: Trigger job 01
          branch: trigger-job-01
          file_pattern: CI_LAST_RUN.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        if: always()
